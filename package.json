{
  "name": "iden-app",
  "version": "1.0.0",
  "description": "IDEN — LinkedIn Personal Brand OS",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}
